---
presets:
  defaults:
    filters:
      S3Object:
        - "solidblocks-test"
      IAMUser:
        - "admin"
        - "solidblocks-test"
      IAMUserPolicyAttachment:
        - "admin -> AdministratorAccess"
      IAMUserAccessKey:
        - "admin -> AKIA5VENMCB5IINZHWEO"
        - "solidblocks-test -> AKIA5VENMCB5MTY25IZJ"

regions:
  - "eu-central-1"
  - "global"

blocklist:
  - 938746843251

resource-types:
  excludes:
    - S3Object # Excluded because S3 bucket removal handles removing all S3Objects
  targets:
    - S3Bucket
    - DynamoDBTable
    - IAMUserPolicy
    - IAMUserAccessKey
    - IAMUser
    - ECRRepository

accounts:
  938746843258:
    presets:
      - defaults
