---
dependencies:
  - role: dependency1

galaxy_info:
  description: A role to create a standalone PostgreSQL server

argument_specs:
  main:
    short_description: setup PostgreSQL database
    options:
      variable1:
        description: description for variable1
        required: true
      variable2:
        description: description for optional variable2
        required: false
      variable3:
        description: description for variable3 with value from defaults/main.yml
      variable4:
        description: description for variable4 with default
        default: "variable4 default"
      variable5:
        description: description for variable5 with value from vars/main.yml
