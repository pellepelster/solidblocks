---
dependencies:
  - role: dependency1

galaxy_info:
  description: A role to create a standalone PostgreSQL server

argument_specs:
  main:
    short_description: setup PostgreSQL database
    options:
      var1:
        description: meta information for var1
        required: true

      default1:
        description: meta information for default1
        required: true

