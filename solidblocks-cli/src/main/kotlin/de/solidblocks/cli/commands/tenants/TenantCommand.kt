package de.solidblocks.cli.commands.tenants

import com.github.ajalt.clikt.core.CliktCommand

class TenantCommand : CliktCommand(name = "tenant", help = "manage tenants") {
    override fun run() {
    }
}
