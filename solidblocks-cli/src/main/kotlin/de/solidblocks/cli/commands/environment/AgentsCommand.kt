package de.solidblocks.cli.commands.environment

import com.github.ajalt.clikt.core.CliktCommand

class EnvironmentAgentsCommand : CliktCommand(name = "agents", help = "manage agents") {
    override fun run() {
    }
}
