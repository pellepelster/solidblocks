package de.solidblocks.cli.commands.environments

import com.github.ajalt.clikt.core.CliktCommand

class EnvironmentCommand : CliktCommand(name = "environment", help = "manage environments") {
    override fun run() {
    }
}
