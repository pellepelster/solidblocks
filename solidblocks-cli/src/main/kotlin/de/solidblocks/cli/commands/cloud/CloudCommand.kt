package de.solidblocks.cli.commands.cloud

import com.github.ajalt.clikt.core.CliktCommand

class CloudCommand : CliktCommand(name = "cloud", help = "manage clouds") {
    override fun run() {
    }
}
