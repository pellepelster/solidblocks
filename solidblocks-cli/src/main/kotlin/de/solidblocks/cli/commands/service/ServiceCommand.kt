package de.solidblocks.cli.commands.service

import com.github.ajalt.clikt.core.CliktCommand

class ServiceCommand : CliktCommand(name = "service", help = "manage services") {
    override fun run() {
    }
}
