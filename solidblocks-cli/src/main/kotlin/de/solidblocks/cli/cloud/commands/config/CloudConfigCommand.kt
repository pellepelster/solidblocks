package de.solidblocks.cli.cloud.commands.config

import com.github.ajalt.clikt.core.CliktCommand

class CloudConfigCommand : CliktCommand(name = "config", help = "manage cloud configurations") {
    override fun run() {
    }
}
