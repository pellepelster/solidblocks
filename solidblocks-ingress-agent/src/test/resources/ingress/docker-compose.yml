version: '3.5'

services:

  backend-service1:
    build: ./backend-service1
    ports:
      - "443"
    environment:
      - SERVER_CERT
      - SERVER_KEY
    networks:
      solidblocks-dev:
        aliases:
          - backend-service1.tenant1.dev.local.test

networks:
  solidblocks-dev:
    name: solidblocks-dev